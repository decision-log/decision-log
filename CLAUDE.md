# decision-log

## Agent skills

### Issue tracker

이슈는 GitHub Issues (`uwonu606/decision-log`) 에서 `gh` CLI 로 다룬다. See `docs/agents/issue-tracker.md`.

### Triage labels

기본 다섯 개 라벨을 이름 그대로 쓴다. See `docs/agents/triage-labels.md`.

### Domain docs

single-context — 루트 `CONTEXT.md` + `docs/adr/`. See `docs/agents/domain.md`.
